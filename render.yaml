services:
  - type: web
    name: online-portfolio
    env: node
    buildCommand: npm install
    startCommand: npx serve .
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
    healthCheckPath: / 
